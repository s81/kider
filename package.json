{
  "name": "sprout",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "packageManager": "bun@1.3.14",
  "scripts": {
    "test": "vitest run",
    "build": "bun run --cwd apps/ide build",
    "typecheck": "bun run --cwd apps/ide typecheck"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "3.2.6",
    "eslint": "^9.28.0",
    "typescript": "^5.8.3",
    "vitest": "3.2.6"
  }
}
